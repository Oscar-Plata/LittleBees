<div>
  <h1>Productos</h1>
  <h2>Productos Disponibles: <%= @productos.count %></h2>
  <%= link_to 'Añadir Producto', new_producto_path %>
</div>
<br/>
<%= form_with url: productos_path, method: :get do |form| %>
  <%= form.text_field :busqueda, placeholder: "Nombre del producto", value: params[:busqueda] %>
  <%= form.submit "Buscar", disable_with: 'Submiting...' %>
<% end %>
<br />

<%= render partial: 'producto', collection: @productos %>
